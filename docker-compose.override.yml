# Development overrides
services:
  streaming-server:
    environment:
      - RELOAD=true
      - DEBUG=true
      - LOG_LEVEL=debug
    command: ["python", "/app/src/api/streaming_server.py"]

  nginx:
