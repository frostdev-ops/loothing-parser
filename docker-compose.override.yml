# Development overrides
services:
  streaming-server:
    environment:
      - RELOAD=true
      - DEBUG=true
      - LOG_LEVEL=debug
    command: ["python", "-m", "src.api.streaming_server"]

  nginx:
